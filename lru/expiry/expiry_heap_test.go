package expiry

import (
	"testing"
	"time"
)

func TestExpiryTracker(t *testing.T) {
	baseTime := time.Now()

	for _, tbl := range []struct {
		name string
	}{} {
		t.Run(tbl.name, func(t *testing.T) {
		})
	}

}
